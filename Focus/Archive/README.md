# Focus Archive

This folder is populated by [[Focus/Archive Tool]]. Each fully-completed year gets one summary note here (`{year} Summary.md`) with pre-computed totals, plus a `Sessions/{year}/` subfolder holding the original raw session notes moved out of the active `Focus/Sessions/` folder.

Nothing here needs manual editing — the summary notes are generated automatically when you run the Archive Tool.
