---
month: 2026-05
quarter: 2026-Q2
year: 2026
---

# 📅 Monthly Review

> Month: **May 2026**
> Part of [[2026-Q2]]

---

# 🎯 Monthly Goal

- Push the ITAM portal past the RBAC + Graph API milestone.

---

# 📊 Weekly Reviews

```dataview
LIST
FROM "Journal/Weekly"
WHERE month = this.month
SORT file.name ASC
```

---

# 🧘 Habits

```dataviewjs
const days = dv.pages('"Journal/Daily"').where(p => p.month === dv.current().month);
const total = days.length;
const habits = ["meditation", "exercise", "reading", "better"];
const labels = { meditation: "🧘 Meditation", exercise: "💪 Exercise", reading: "📖 Reading", better: "🚀 1% Better" };

const rows = habits.map(h => {
  const done = days.where(p => p[h] === true).length;
  const pct = total > 0 ? Math.round((done / total) * 100) : 0;
  return [labels[h], `${done}/${total}`, `${pct}%`];
});

dv.table(["Habit", "Days", "Completion %"], rows);
```

---

# 📖 Journal Activity

```dataview
TABLE
date AS Date,
choice(meditation,"✅","❌") AS "🧘",
choice(exercise,"✅","❌") AS "💪",
choice(reading,"✅","❌") AS "📖",
choice(better,"✅","❌") AS "🚀"
FROM "Journal/Daily"
WHERE month = this.month
SORT date ASC
```

---

# 💪 Fitness

```dataviewjs
const days = dv.pages('"Journal/Daily"').where(p => p.month === dv.current().month);
const total = days.length;
const exercised = days.where(p => p.exercise === true).length;
const pct = total > 0 ? Math.round((exercised / total) * 100) : 0;
dv.paragraph(`**Exercise:** ${exercised}/${total} days (${pct}%) — see [[Fitness/Progress]] for the full chart.`);
```

---

# 📚 Reading

```dataviewjs
const days = dv.pages('"Journal/Daily"').where(p => p.month === dv.current().month);
const totalPages = days.array().reduce((a, p) => a + (p.pages_read || 0), 0);
const books = new Set(days.where(p => p.book_title && p.book_title.length > 0).array().map(p => p.book_title));
dv.paragraph(`**Pages read:** ${totalPages} across ${books.size} book(s) — see [[Reading/Reading Log]] for full totals.`);
```

---

# 🍅 Focus Time This Month

```dataview
TABLE WITHOUT ID
project AS "Project",
sum(rows.duration_min) AS "Total Minutes",
length(rows) AS "Sessions"
FROM "Focus/Sessions"
WHERE month = this.month
GROUP BY project
SORT sum(rows.duration_min) DESC
```

---

# 🏆 Biggest Achievement

- Migrated VCreaTek Pulse off the last legacy HTML page.

---

# 😕 Biggest Challenge

- Underestimated how long the Entra ID SSO integration would take.

---

# 💡 Lessons Learned

- A short walk before meditation makes the 5 minutes actually stick.

---

# 🎯 Goals for Next Month

- Push the ITAM portal past the RBAC + Graph API milestone.
